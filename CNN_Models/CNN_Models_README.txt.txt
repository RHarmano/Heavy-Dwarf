These scripts construct, train and test the CNN models.
To run these scripts specify the path to access the pre-processed data .npy file
Specify the model architecture in the subclassed model
Run the model either by running the script in an IDE or in the command window